#EXTRA_DIST = reconf Makefile.autogen Makefile.autogensimp libtoolcreate.sh
EXTRA_DIST =
SUBDIRS = 

moddir = $(prefix)/mod/
mod_DATA= irqk.ko edmak.ko cmemk.ko loadmodules.sh 
#install-data-hook:
#	if ! test -e $(prefix)/images; then \
#	mkdir $(prefix)/images; \
#	fi

